env:
  global:
    - JWTSECRETKEY=mynameisniwemuhuza_mireille
language: node_js
node_js: 
  - "stable"
cache:
  directories:
    - "node_modules"
after_script:
- "npm run test:cov"
- "npm install coveralls && cat ./coverage/lcov.info | coveralls"